package com.example.demo.leetcode.hot100;

public class StringToIntegerAtoi {
    /**
     * 力扣题目 8：字符串转换整数 (atoi)
     *
     * 请你来实现一个 myAtoi(string s) 函数，使其能将字符串转换成一个 32 位有符号整数。
     * 函数 myAtoi(s) 的算法如下：
     *  1. 读入字符串并丢弃无用的前导空格
     *  2. 检查下一个字符（假设还未到字符串末尾）为正还是负号，读取该字符（如果有）。
     *  3. 读取下一字符，直到到达下一个非数字字符或到达输入的末尾。
     *  4. 将这些数字转换为整数。
     * 若整数超出 32 位有符号整数范围，则返回边界值。
     *
     * 示例 1：
     * 输入：s = "42"
     * 输出：42
     *
     * 示例 2：
     * 输入：s = "   -42"
     * 输出：-42
     *
     * 示例 3：
     * 输入：s = "4193 with words"
     * 输出：4193
     */

    public int myAtoi(String s) {
        return 0;
    }

    public static void main(String[] args) {
    }
}
